<section class="relative s:h-screen s:sticky s:top-80 {{ item.background_color ? 'bg-' ~ item.background_color|lower : 'bg-yellow' }}">
    {# <pre>{{ item|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre> #}

    <div class="site-max site-grid h-full">
        
            <div class="order-2 s:order-none col-start-1 col-span-full s:col-span-5 py-32 s:py-65 s:pl-32 flex flex-col items-start justify-between s:h-[calc(100vh-8rem)]">
                <div class="relative flex flex-col mb-20 space-y-40">
                    <p class="text-grey text-12 font-medium leading-none tracking-[0.12rem] uppercase">{{ item.subtitle }}</p>
                    <h2 class="text-32 s:text-40 font-normal leading-[1.16] tracking-[-0.04rem] text-green">{{ item.title }}</h2>
                </div>
                
                <div class="relative space-y-40">
                    <div class="relative text-green">
                        <p class="text-16 font-normal leading-[1.32]">{{ item.description }}</p>
                    </div>

                    <div class="relative">
                        <a class="relative inline-flex space-x-8 items-center justify-center text-12 font-medium leading-none tracking-[0.12rem] text-yellow py-15 px-25 bg-black uppercase" href="{{ item.link.url }}" target="{{ item.link.target }}">
                            <span class="inline-flex">
                                {{ item.link.title }}
                            </span>
                            <span class="inline-flex">
                                <svg class="w-8 h-11" viewBox="0 0 8 11" fill="none" xmlns="http://www.w3.org/2000/svg">
                                    <g clip-path="url(#clip0_5001_23171)">
                                        <path d="M1.73438 1L6.23438 5.5L1.73438 10" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
                                    </g>
                                    <defs>
                                        <clipPath id="clip0_5001_23171">
                                            <rect width="7" height="11" fill="currentColor" transform="translate(0.734375)"/>
                                        </clipPath>
                                    </defs>
                                </svg>
                            </span>
                        </a>
                    </div>
                </div>
            </div>

            <div class="order-1 s:order-none col-start-1 col-span-full s:col-start-7 s:col-span-10 -mx-32">
                <figure class="relative w-full h-full">
                    {% include "blocks/common/image.twig" with {
                        props : {
                            data: {
                                image : item.image
                            },
                            options: {
                                container_class: 'w-full h-full overflow-hidden',
                                class : 'w-full h-full object-cover',
                                size: 'full',
                                lazy: true,
                                aspect_ratio_mobile: '101%',
                            }
                        }
                    } %}
                </figure>
            </div>
        </div>
    
</section>